# WORK LOG - 2026-04-20

## Today's Summary
- 0 commits (changes uncommitted, awaiting user review)
- 1 project touched: `aruco_drone_nav`
- Built .BIN dataflash log analysis into `tools/glb_viewer.html` — in-browser parser, successful-flight auto-selection, actual vs commanded path overlay, reached-WP timing table, and a new **Compare Flights** tab for cross-mission Excel-style comparison with per-column normalized green→red heat-map coloring.

---

## Commits (0)

_No commits today — all work is uncommitted in `aruco_drone_nav` working tree._

## Progress

- **~15:00** Phase 1 complete: pure-JS ArduPilot `.BIN` parser (FMT-bootstrapped, decodes POS/CMD/EV/MODE/MSG/GPS/ORGN/PARM), `findSuccessfulFlight` (ARM → NOT_LANDED → LAND_COMPLETE segmentation that ignores ground arms / failed pre-arms), 3D overlay of actual flight path (green Line2) + commanded waypoints (color-coded octahedrons) + commanded-order dashed line, side panel with summary card + reached-WP table (timing, NED, lat/lon, segment duration, planned/actual loiter dwell).
- **~15:45** End-to-end validated against `00000005.BIN` via headless Chromium: 13 reached WPs, flight time 180.4s, actual path 94.6m vs commanded 40.6m, loiter #11 planned 30.0s / actual 34.7s. Fixed `Travel` column bug (was measuring `t_mission_start - t_prev_reached` ≈ 0; now `t_reached - t_prev_reached` segment duration).
- **~16:15** Docs updated per repo rule: `docs/GLB_VIEWER.md` (new BIN input row, workflow #3 "Analyze a flight log", BIN layer legend, `B` keyboard shortcut), `CLAUDE.md` (tools table), `README.md` (tools table + last-updated date).
- **~16:45** Phase 2 complete: new **Compare Flights** tab inside `glb_viewer.html`. Tab bar in navbar switches between 3D View and Compare. `computeFlightMetrics()` extracted from render pipeline so both tabs share the same numbers. Cross-mission-safe columns: flight time (↓), armed (neutral), actual path (↓), cmd path (neutral), path ratio (↓), reached fraction (↑), loiter err (↓), max alt (neutral), avg speed (↑). Per-column min/max normalization → green→yellow→red heat-map fill (neutral columns uncolored). Drag-and-drop + multi-select file input + per-row × removal. Verified against `00000005.BIN` vs `00000019.BIN`: #19 wins 6 comparable columns, #5 wins loiter err.
- **~17:00** Cleanup: deleted test harnesses, stopped background HTTP server.

---

## Stats

| Metric | Value |
|--------|-------|
| Commits | 0 |
| Projects | 1 |
| Repos | aruco_drone_nav |
| Lines changed | ~+300 in `tools/glb_viewer.html`, doc updates in 3 files |
