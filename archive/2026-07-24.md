# WORK LOG - 2026-07-24

## Today's Summary
- 5 commits in SoundVisualizer
- Pi rebuilt as immutable rig: overlayroot rootfs + persistent 2.4G data partition (SD surgery: shrink rootfs, drop 2G swapfile, zram-only). Config, dist bundle, measurements + backup repo all live on the data partition via symlinks.
- Thrust stand calibrated against 200.0 g reference: −4.3% → +0.15% (cal_thrust −1.00199), two iterations.
- MotorTest tuned (8 ramps, PWM 1120, 3s countdown); first drone-paczek capture taken (dp1-baseline); RPM=0 diagnosed as unhooked probe (4-in-1 ESC — next time: optical probe or ESC telemetry).

---

## Commits (5)

### SoundVisualizer (5)
- [21bd3bc] MotorTest: 8 ramps, PWM 1120, 3s clear-the-prop countdown after Start
- [361e97f] Docs: thrust cal corrected vs 200g reference; config.toml lives on data partition
- [1188437] Docs: OS is Raspberry Pi OS (Debian trixie base), not vanilla Debian
- [4f03aa3] Docs: immutable-rootfs architecture (overlayroot + persistent data partition)
- [1eabc96] Docs: Pi reflashed as sound-viz (user pi), overlay-FS operating model

## Progress

- **18:15** Regained access to reflashed Pi (sound-viz / pi / dupa), restored full stack: config, udev mic names, 11-mic preset + cals, backup deploy key, cutoffs. SD partition surgery + overlayroot=tmpfs:recurse=0 (the recurse flag is critical — default overlays the data partition ro). Thrust cal verified 200.30 g vs 200.00 g. Ephemeral-rootfs gotcha bit twice (dist reverted on reboot) → dist moved to data partition. Flaky eth cable dropped the link ~10× — replace it.

---

## Stats

| Metric | Value |
|--------|-------|
| Commits | 5 |
| Projects | 1 |
| Repos | SoundVisualizer |
